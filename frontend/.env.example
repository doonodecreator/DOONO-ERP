# Leave empty when the frontend and Laravel API share the same origin/tunnel.
# For a separate backend, set the public API origin including /api/v1.
# Example: VITE_API_URL=https://your-backend.example.com/api/v1
VITE_API_URL=
