Screenshot inspection findings, 2026-08-18:

1. Subscription page: the page renders only the heading "Subscription" and subtitle "Manage your school's access." with a large blank area. No subscription card, plan, status, expiry, renewal, upgrade, or error message is visible. The role badge says Proprietor.

2. Fees & Student Invoices page: the page renders the heading, search/filter controls, and "No fee records found." The "Configure Fee Setup" control is visibly greyed/low-contrast and appears non-functional. The role badge says Proprietor. The page is not showing a loading or explicit API error state, so the failure may be a route/page mapping or a failed data request that is being swallowed.

3. Both screenshots show the same mobile layout and the same active school role badge, indicating the issue is occurring inside the school context rather than at login.

Next: inspect the remaining screenshots, then trace the Fees button's setPage target, App.jsx switch cases, FeesAndPayments page, and backend fee routes/permissions.

4. Students Directory screenshot shows a concrete production backend error: `SQLSTATE[42S02]` / MySQL says table `railway.school_setup_delegations` does not exist. The failing query originates in `User::hasPermission()` checking delegated `manage_students` permission for school_id 3 and user_id 10. This is a deployment/database-schema mismatch, not a student-page-only problem. The page correctly displays the error state and Retry button, but permission checks can break many school pages.

5. Classes & Academic Streams screenshot shows the Add New Class modal is open, with a native-looking `Select Division...` prompt above it. The modal fields are visible but controls appear low-contrast/disabled-looking. This is likely downstream of missing divisions data or permission/API errors; it must be checked after the missing delegation table is resolved. The screenshot confirms the UI action is reaching the class modal, so the button itself is not universally dead.

6. Divisions page shows the same production error before any division data can load: `school_setup_delegations` table missing while checking `manage_divisions`. Therefore the Add Division button may render but the page cannot complete its permission/data request.

7. Leadership & Role Invitations page also shows a direct error loading delegated setup records: query selects from `school_setup_delegations` for school_id 3. This confirms the missing table is a single shared blocker affecting students, divisions, leadership, classes, and likely every controller/page that calls User::hasPermission().

8. Terms page fails in exactly the same way while checking `manage_terms`; the Add Term form is visible but the page-level permission request fails first.

9. Academic Sessions page fails in the same way while checking `manage_academic_sessions`. The repeated `school_setup_delegations` missing-table error proves the root cause is infrastructure/schema drift on Railway, not eleven independent broken buttons. The greyed-looking controls also reflect mobile styling/disabled state, but the backend error is the primary blocker.

10. Screenshot nine duplicates the academic-session error and adds no new root cause.

11. Screenshot ten reveals a second missing production table: `railway.role_invitations` does not exist when Leadership & Role Invitations loads. Thus Railway is missing at least two migrations/tables: `school_setup_delegations` and `role_invitations`. This explains why leadership and setup permissions fail even after code deployment.
